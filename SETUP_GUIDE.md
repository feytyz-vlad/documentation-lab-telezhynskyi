# Посібник з встановлення та запуску

Цей сервіс не потребує складної інсталяції та працює прямо у браузері.

## 🛠 Необхідні інструменти
- Будь-який сучасний браузер (Chrome, Firefox, Edge).
- Доступ до Інтернету.
- GitHub акаунт (лише якщо ви запускаєте проєкт локально).

## 📥 Встановлення (локальна версія)
1. Склонуйте репозиторій:
git clone https://github.com/<username>/documentation-lab-<прізвище>.git

markdown
Копировать код
2. Перейдіть у директорію:
cd securepass-generator

markdown
Копировать код
3. Відкрийте файл `index.html`.

## ▶️ Запуск
1. Відкрийте файл у браузері.
2. Інтерфейс відобразиться автоматично.

## ✔ Перевірка роботи
- Натисніть кнопку **Generate**.
- Якщо система згенерувала пароль — усе працює правильно.
- Якщо ні — перевірте консоль браузера на наявність помилок (`F12 → Console`).
